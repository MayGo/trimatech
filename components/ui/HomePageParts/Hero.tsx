import { Box } from '@chakra-ui/react';

export const Hero = () => {
    return (
        <Box bg="bgColor" w="full" h="600px" roundedBottom={20} p={4 * 4}>
            <Box>Hero</Box>
        </Box>
    );
};
